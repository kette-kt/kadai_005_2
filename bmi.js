// 定数の宣言(体重)
let userWeight;
// 値の代入
userWeight = 68;

// 変数の宣言・値の代入(身長)
let userHight = 1.7;

// 変数の宣言・値の代入(BMI)
let bmi = userWeight / (userHight * userHight);

//コンソールへの出力
console.log(bmi);


